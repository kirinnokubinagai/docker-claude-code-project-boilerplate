{
  "mcpServers": {
    "mcp-gateway": {
      "command": "sh",
      "args": ["-c", "cd /Users/kirinnokubinagaiyo/Project/mcp-gateway && bun server/index.ts"],
      "env": {
        "MCP_PROXY_PORT": "9999"
      }
    },
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GITHUB_TOKEN}"
      }
    }
  }
}
